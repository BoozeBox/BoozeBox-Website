
// Entrance animations 



// Button animations

// let productBtn = document.querySelector("#shop .product-btn");
// let productTxt = document.querySelector(".product-btn span");
// let productArw = document.querySelector(".arrow");

// productBtn.addEventListener("mouseenter", function() {
//     gsap.to(productBtn, { top: "-10%", ease: "back.inOut" });
//     gsap.to(productTxt, { scale: 1.2, ease: "back.inOut" });
//     gsap.to(productArw, { scale: 1.2, ease: "back.inOut" });
// })

// productBtn.addEventListener("mouseleave", function() {
//     gsap.to(productBtn, { top: "-12.5%", ease: "back.inOut" });
//     gsap.to(productTxt, { scale: 1, ease: "back.inOut" });
//     gsap.to(productArw, { scale: 1, ease: "back.inOut" });
// })
